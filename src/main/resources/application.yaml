spring:
  application:
    name: daouoffice-agent
  ai:
    model:
      chat: ollama
    openai:
      api-key: ${OPENAI_API_KEY:test-key}

agent:
  llm-provider: ollama
  max-loop: 5
  recent-message-count: 12
